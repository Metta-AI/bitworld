# ECR repositories are now created dynamically by docker_build.nim on first push.
# Existing repos (treeform/games/crewrift, treeform/players/notsus) remain in AWS
# but are no longer managed by terraform.
